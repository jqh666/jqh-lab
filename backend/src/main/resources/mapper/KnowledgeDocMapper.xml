<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jqhlab.repository.KnowledgeDocMapper">

    <insert id="insertKnowledgeDoc" parameterType="com.jqhlab.entity.KnowledgeDoc" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO knowledge_docs (title, source, content, embedding, created_at)
        VALUES (#{title}, #{source}, #{content}, CAST(#{embedding} AS vector), #{createdAt})
    </insert>

    <!-- pgvector similarity search -->
    <select id="findSimilar" resultType="com.jqhlab.entity.KnowledgeDoc">
        SELECT id, title, source, content, created_at
        FROM knowledge_docs
        WHERE embedding IS NOT NULL
        ORDER BY embedding &lt;#> CAST(#{vector} AS vector) ASC
        LIMIT #{topK}
    </select>

</mapper>
